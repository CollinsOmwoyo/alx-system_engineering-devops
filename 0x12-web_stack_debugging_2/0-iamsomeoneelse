#!/usr/bin/env bash
# Run the whoami command as the user passed as an argument

USER="$1"

if id "$USER" &>/dev/null; then
    sudo -Hu "$USER" env | grep HOME  # Debugging step (optional)
    sudo -Hu "$USER" whoami
else
    echo "Error: User '$USER' does not exist." >&2
    exit 1
fi
